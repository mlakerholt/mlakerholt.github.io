import {getActive,loadCampaign,escapeHTML as e} from './io.mjs';
import {activeRound} from './data.mjs';
import {getContext} from './desk.mjs';
async function resume(){const node=document.querySelector('#resume');try{const id=await getActive(),c=id&&await loadCampaign(id);if(!c)return;const r=activeRound(c),context=getContext(),matching=context?.campaign===c.id,aliases={campaign:'plan',assay:'plan',equipment:'library',budget:'library',screen:'results',review:'decide'};const stage=matching?(aliases[context.stage]||context.stage):(r.analyses.length||r.sequenceDatasets?.length)?'results':'plan';const url=matching?context.url:'planner.html?'+new URLSearchParams({campaign:c.id,round:r.id})+'#'+stage;node.innerHTML=`<div><p class="eyebrow">Continue</p><h2>${e(c.name)}</h2><p>Round ${r.number} · ${e(r.brief.parent||'Parent not defined')}</p></div><a class="button-like primary" href="${e(url)}">Resume →</a>`;}catch{node.innerHTML='<div><h2>Your saved copy needs attention</h2><p>Open the planner to review recovery options.</p></div><a class="button-like primary" href="planner.html">Open planner →</a>';}}
resume();
